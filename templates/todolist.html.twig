<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Todo-list</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

</head>
<body>
<h2>My todo-list</h2>
 <div class="mb-3">
    <a href="{{ path('add_action') }}" class="btn btn-primary">Add New Action</a>
 </div>

<ul class="list-group" id="todoList">
    <!-- Creating list elements for each action, with links to update or delete by it's id -->
    {% for action in actions %}
    <li class="list-group-item">
        <input class="form-check-input me-1" type="checkbox" value="" id={{ action.id }}>
        <label class="form-check-label" for={{ action.id }}>{{ action.action }} - Due: {{ action.dueDate|date('Y-m-d') }}</label>
        <a href="{{ path('remove_action', {'id': action.id}) }}">Delete</a>
        <a href="{{ path('edit_action', {'id': action.id}) }}">Update</a>
    </li>
    {% endfor %}
</ul>

</body>
</html>
